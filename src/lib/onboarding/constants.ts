/** Persists chosen profile path if user drops off before login completes. */
export const ONBOARDING_NEXT_KEY = "qrnetra_onboarding_next";

/** Last profile type chosen on /create/type (vehicle | child | pet | business). */
export const SELECTED_PROFILE_TYPE_KEY = "selectedProfileType";
