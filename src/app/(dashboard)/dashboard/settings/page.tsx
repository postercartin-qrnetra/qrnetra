import { StubPage } from "@/components/stub-page";

export default function SettingsPage() {
  return (
    <StubPage
      title="Settings"
      description="Notifications, active sessions, delete account — Supabase session APIs next."
    />
  );
}
