import { StubPage } from "@/components/stub-page";

export default function OrdersPage() {
  return (
    <StubPage
      title="Orders"
      description="Paid orders and invoices — Razorpay webhook + `orders` table next."
    />
  );
}
