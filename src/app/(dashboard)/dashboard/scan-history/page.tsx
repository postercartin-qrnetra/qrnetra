import { StubPage } from "@/components/stub-page";

export default function ScanHistoryPage() {
  return (
    <StubPage
      title="Scan history"
      description="Aggregate timeline from `scan_events` — filters and export later."
    />
  );
}
